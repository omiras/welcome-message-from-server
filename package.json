{
  "name": "welcome-message-from-server",
  "version": "1.0.0",
  "description": "We want to build a simple server. The server should render a form to the user. After some data is provided and sent back to the server; the user should receive a welcome message.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "greetings": "^1.0.0",
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node": "12.x"
  }
}
